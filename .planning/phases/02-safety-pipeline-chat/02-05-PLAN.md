---
phase: 02-safety-pipeline-chat
plan: 05
type: execute
wave: 1
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - backend/app/api/chat.py
autonomous: true
gap_closure: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "get_safety_pipeline() returns a SafetyPipeline with real scanners (DataProtectionScanner, LLMGuardrailScanner) when dependencies are available"
    - "If scanner dependencies fail to import, pipeline still starts with available scanners or empty list (graceful degradation)"
  artifacts:
    - path: "backend/app/api/chat.py"
      provides: "get_safety_pipeline() wiring real scanners with fallback"
      exports: ["get_safety_pipeline"]
---

<objective>
Wire real scanners (DataProtectionScanner, LLMGuardrailScanner) into get_safety_pipeline() with graceful try/except fallback so the production server actually blocks unsafe content.

This closes the single verification gap: scanners exist but are not injected into the pipeline dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-safety-pipeline-chat/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire real scanners into get_safety_pipeline() with graceful fallback</name>
  <files>backend/app/api/chat.py</files>
  <read_first>
    - backend/app/api/chat.py (current get_safety_pipeline at line 57-60)
    - backend/app/safety/data_protection.py (DataProtectionScanner class and __init__ signature)
    - backend/app/safety/llm_guardrails.py (LLMGuardrailScanner class and __init__ signature)
    - backend/app/safety/torch_compat.py (must be imported before llm_guardrails to apply monkey-patch)
    - backend/app/safety/scanner_interface.py (Scanner protocol)
  </read_first>
  <behavior>
    - get_safety_pipeline() tries to instantiate DataProtectionScanner and LLMGuardrailScanner
    - Each scanner import/init is wrapped in its own try/except so one failing doesn't prevent the other
    - torch_compat must be imported before LLMGuardrailScanner to apply the torch.jit monkey-patch
    - If a scanner fails to init (missing deps, model download failure), log a warning and skip that scanner
    - If all scanners fail, pipeline still starts with scanners=[] (existing fail-closed semantics handle this)
    - structlog or logging used for warnings about unavailable scanners
  </behavior>
  <action>Replace get_safety_pipeline() in backend/app/api/chat.py:

1. Import logging (use structlog if available, else standard logging)
2. In get_safety_pipeline(), build scanners list:
   - Try: import and instantiate DataProtectionScanner, append to scanners
   - Try: import torch_compat first (for monkey-patch), then import and instantiate LLMGuardrailScanner, append to scanners
   - Each try/except logs warning with scanner name and exception
3. Pass scanners list to SafetyPipeline(scanners=scanners, policy=policy, timeout=30.0)
4. Remove the old comment about "scanners added when deps available"
</action>
  <acceptance_criteria>
    - backend/app/api/chat.py get_safety_pipeline() contains `DataProtectionScanner()` instantiation attempt
    - backend/app/api/chat.py get_safety_pipeline() contains `LLMGuardrailScanner()` instantiation attempt
    - Each scanner instantiation is wrapped in its own try/except block
    - torch_compat is imported before LLMGuardrailScanner
    - `cd backend && .venv/Scripts/python -c "from app.api.chat import get_safety_pipeline; p = get_safety_pipeline(); print(f'Scanners: {len(p._scanners)}')"` exits 0
    - `cd backend && .venv/Scripts/python -m pytest -q --tb=short` all tests still pass
  </acceptance_criteria>
  <verify>
    <automated>cd backend && .venv/Scripts/python -m pytest -q --tb=short && .venv/Scripts/python -c "from app.api.chat import get_safety_pipeline; p = get_safety_pipeline(); print(f'Scanners: {len(p._scanners)}')"</automated>
  </verify>
  <done>get_safety_pipeline() wires real DataProtectionScanner and LLMGuardrailScanner with graceful per-scanner fallback. Production server now blocks unsafe content when dependencies are available.</done>
</task>

</tasks>

<verification>
cd backend && .venv/Scripts/python -c "
from app.api.chat import get_safety_pipeline
p = get_safety_pipeline()
print(f'Pipeline has {len(p._scanners)} scanner(s)')
assert len(p._scanners) >= 1, 'Expected at least one real scanner wired'
print('Gap closure verified: real scanners wired in production pipeline')
"
</verification>

<success_criteria>
- Production get_safety_pipeline() returns SafetyPipeline with real scanners
- Graceful fallback if dependencies unavailable
- All existing tests still pass
</success_criteria>

<output>
Create .planning/phases/02-safety-pipeline-chat/02-05-SUMMARY.md when done
</output>
