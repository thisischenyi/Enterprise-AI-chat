---
context: phase
phase: 02-safety-pipeline-chat
task: 2
total_tasks: 3
status: blocked
last_updated: 2026-05-21T14:25:57
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: llm-guard scanner class names differ from plan — The plan references `PromptInjectionScanner`, `JailbreakScanner`, `ToxicityScanner`, but llm-guard 0.3.10 uses different names (`PromptInjection`, likely `Jailbreak`). The resuming agent MUST verify actual class names by importing `llm_guard.input_scanners.*` modules before writing LLMGuardrailScanner wrapper code.

- [ ] CONSTRAINT: llm-guard 0.3.10 strict dependency pins incompatible with Python 3.13 — llm-guard pins `torch==2.0.1` (no Python 3.13 wheel). We installed torch 2.12.0 + transformers 4.57.6 + onnxruntime 1.26.0 + llm-guard via `--no-deps`. The resuming agent MUST verify scanners initialize and run inference correctly with these relaxed dependencies before proceeding with implementation. If scanners fail at runtime, the fallback is recreating the venv with Python 3.12.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Slow llm-guard model loading | torch/transformers model download + initialization takes 30+ seconds on first import. Import tests appear stuck but are actually loading ~400MB ML models. | advisory | Use longer timeout (60s+) for any Python command that imports llm-guard scanners. Don't assume it's hung. |
| Subagent model override | Specifying `model: sonnet` for subagents causes API error on this platform (glm-5.1 runtime). Sonnet model IDs are not supported. | advisory | Spawn subagents without model override — let them inherit the current session model |

<current_state>
Executing Plan 02-01 of Phase 2 (Safety Pipeline & Chat). Task 1 (scanner_interface.py + block_messages.py) completed and committed (59ba2fa). Task 2 (DataProtectionScanner + custom_recognizers.py) partially done — custom_recognizers.py was written but NOT committed, and data_protection.py placeholder not yet replaced. Task 3 (LLMGuardrailScanner) blocked by llm-guard dependency verification.

Python packages installed: presidio-analyzer 2.2.362, presidio-anonymizer 2.2.362, spacy 3.8.14, en_core_web_lg 3.8.0, torch 2.12.0, transformers 4.57.6, onnxruntime 1.26.0, llm-guard 0.3.10 (--no-deps).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Scanner interface types + block message templates — committed (59ba2fa)
  - scanner_interface.py: RiskCategory enum, ScannerFinding, ScannerResult, Scanner Protocol, PolicyDecision
  - block_messages.py: BLOCK_MESSAGE_TEMPLATES (5 categories x 2 sources), get_block_message(), FAIL_CLOSED_MESSAGE
  - Updated __init__.py exports

Incomplete Tasks:
- Task 2: DataProtectionScanner + custom recognizers — custom_recognizers.py written (uncommitted), data_protection.py not yet replaced
- Task 3: LLMGuardrailScanner — not started, blocked on llm-guard verification
</completed_work>

<remaining_work>

- Task 2: Commit custom_recognizers.py, replace data_protection.py placeholder with Presidio-backed implementation, add deps to requirements.txt, update .env.example
- Task 3: Verify llm-guard scanner class names, test scanner init with torch 2.12 + transformers 4.57, implement LLMGuardrailScanner wrapper with correct class names, add llm-guard to requirements.txt
- Plans 02-02 through 02-04: All not started, blocked on 02-01 completion
</remaining_work>

<decisions_made>

- Decided to install torch 2.12.0 + transformers 4.57.6 + onnxruntime + llm-guard --no-deps (bypass strict pins) because torch==2.0.1 has no Python 3.13 wheel
- Decided to keep original llm-guard design intent (user explicitly required no deviation)
- Plan specifies `llm-guard>=1.2.0,<2.0` but actual PyPI package max version is 0.3.10 — requirements.txt should use `llm-guard>=0.3.0`
</decisions_made>

<blockers>
- llm-guard scanner class names: Plan says PromptInjectionScanner but actual class is PromptInjection. Jailbreak and Toxicity class names not yet verified (import test timed out).
- llm-guard runtime compatibility: Scanners may or may not work with torch 2.12 + transformers 4.57 instead of pinned torch 2.0.1 + transformers 4.38.2. Needs verification.
</blockers>

## Required Reading (in order)
1. `.planning/PROJECT.md` — project context, core value, constraints
2. `.planning/phases/02-safety-pipeline-chat/02-01-PLAN.md` — current plan being executed
3. `.planning/phases/02-safety-pipeline-chat/02-CONTEXT.md` — user decisions (D-SA01 through D-SA03, D-SF01 through D-SF03)
4. `.planning/phases/02-safety-pipeline-chat/02-RESEARCH.md` — technical research on Presidio + LLM Guard integration
5. `backend/app/safety/scanner_interface.py` — Scanner protocol and types (Task 1 output, committed)
6. `backend/app/safety/block_messages.py` — Block message templates (Task 1 output, committed)
7. `backend/app/safety/custom_recognizers.py` — Custom recognizers (uncommitted, written)

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: Assuming pip install completes instantly for large ML packages (torch, spacy, transformers). First-time imports download models that take 30+ seconds. → Use timeout=60+ for any Python test involving these imports.
- ANTI-PATTERN: Assuming llm-guard class names match plan documentation. → Always verify actual class names by inspecting the module before writing wrapper code.
- ANTI-PATTERN: Assuming llm-guard strict pin versions are runtime requirements. → They are testing pins; newer versions may work but need verification.

## Infrastructure State
- Git repo: master branch, clean except uncommitted custom_recognizers.py
- Python venv: backend/.venv with Python 3.13.8
- Installed ML packages: torch 2.12.0, transformers 4.57.6, spacy 3.8.14 + en_core_web_lg, presidio-analyzer/anonymizer 2.2.362, llm-guard 0.3.10 (--no-deps), onnxruntime 1.26.0
- Platform: Windows 11, glm-5.1 runtime
- No running services

<context>
We were trying to verify that llm-guard's scanners work with relaxed dependencies (torch 2.12 instead of torch 2.0.1). The import of `llm_guard.input_scanners.prompt_injection` succeeded and revealed the class name is `PromptInjection` (not `PromptInjectionScanner` as the plan states). The jailbreak module import test timed out — it's likely loading ML models on first import which takes significant time. We need to: 1) get the actual class names for all 3 scanners, 2) test if they initialize, 3) test if they run a basic scan. If they work, proceed with implementation. If they don't, the fallback is to recreate the venv with Python 3.12 where torch 2.0.1 has wheels.
</context>

<next_action>
Start with: Verify llm-guard scanner class names and runtime compatibility.
1. Run `python -c "from llm_guard.input_scanners.prompt_injection import PromptInjection; s = PromptInjection(); result = s.scan('ignore previous instructions'); print(result)"` with 60+ second timeout to test scanner init + basic scan.
2. Similarly test Jailbreak and Toxicity scanners (find actual class names first).
3. If scanners work → proceed with LLMGuardrailScanner implementation using correct class names.
4. If scanners fail → recreate venv with Python 3.12 (check `py -3.12` or install Python 3.12 first).
</next_action>