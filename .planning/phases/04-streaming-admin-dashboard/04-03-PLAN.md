---
phase: 04-streaming-admin-dashboard
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - backend/app/db/schema.py
  - backend/app/admin/models_repo.py
  - backend/app/admin/policy_repo.py
  - backend/app/api/admin.py
  - backend/alembic/versions/add_model_policy_configs.py
  - frontend/src/features/admin/ModelsPage.tsx
  - frontend/src/features/admin/PolicyPage.tsx
  - frontend/src/features/admin/components/ModelCard.tsx
  - frontend/src/features/admin/components/ModelEditModal.tsx
  - frontend/src/features/admin/components/ScannerRow.tsx
  - frontend/src/features/admin/components/Toast.tsx
  - frontend/src/features/admin/components/ConfirmDialog.tsx
autonomous: true
requirements: [ADMN-02, ADMN-03]

must_haves:
  truths:
    - "Admin can add, edit, enable/disable model providers with masked API key display"
    - "Admin can enable/disable scanners and set sensitivity levels"
    - "Configuration changes persist in database and take effect at runtime"
    - "API keys are never returned in full from any endpoint"
  artifacts:
    - path: "backend/app/db/schema.py"
      provides: "ModelConfig and PolicyConfig SQLAlchemy models"
      contains: "model_configs"
    - path: "backend/app/admin/models_repo.py"
      provides: "ModelConfig CRUD with encrypted key storage"
      exports: ["ModelConfigRepository"]
    - path: "backend/app/admin/policy_repo.py"
      provides: "PolicyConfig CRUD"
      exports: ["PolicyConfigRepository"]
    - path: "frontend/src/features/admin/ModelsPage.tsx"
      provides: "Model provider card grid with edit modal"
    - path: "frontend/src/features/admin/PolicyPage.tsx"
      provides: "Scanner toggle list with sensitivity sliders"
  key_links:
    - from: "frontend/src/features/admin/ModelsPage.tsx"
      to: "/api/admin/models"
      via: "TanStack useQuery + useMutation"
      pattern: "admin/models"
    - from: "backend/app/admin/models_repo.py"
      to: "backend/app/db/schema.py"
      via: "SQLAlchemy ModelConfig model"
      pattern: "ModelConfig"
---

<objective>
Deliver admin model provider configuration and safety policy/scanner configuration UIs with database-backed persistence.

Purpose: Admins can manage which models are available and how safety scanners behave without code changes (per D-ADM06 through D-ADM11, requirements ADMN-02 and ADMN-03).
Output: Model config CRUD + Policy config CRUD, both backend API and frontend UI.
</objective>

## Phase Goal

**As a** admin, **I want to** configure model providers and safety scanner settings through the dashboard, **so that** I can control which models employees use and how aggressively content is filtered.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-streaming-admin-dashboard/04-RESEARCH.md
@.planning/phases/04-streaming-admin-dashboard/04-UI-SPEC.md
@.planning/phases/04-streaming-admin-dashboard/04-02-SUMMARY.md
@backend/app/db/schema.py
@backend/app/api/admin.py
@backend/app/providers/registry.py
@backend/app/safety/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB tables + backend CRUD for model and policy config</name>
  <files>backend/app/db/schema.py, backend/app/admin/models_repo.py, backend/app/admin/policy_repo.py, backend/app/api/admin.py, backend/alembic/versions/add_model_policy_configs.py</files>
  <read_first>backend/app/db/schema.py, backend/app/api/admin.py, backend/app/providers/registry.py, backend/app/safety/pipeline.py</read_first>
  <action>
    Add to backend/app/db/schema.py per RESEARCH.md table definitions:
    - ModelConfig table: id (UUID PK), name (str 100), provider_type (str 50: "qwen"|"openai_compatible"), endpoint_url (str 500), api_key_encrypted (str 500), model_id (str 100, unique), enabled (bool default True), created_at (datetime)
    - PolicyConfig table: id (UUID PK), scanner_name (str 100, unique), enabled (bool default True), sensitivity (str 20: "low"|"medium"|"high", default "medium"), updated_at (datetime)

    Create Alembic migration for the two new tables.

    models_repo.py — ModelConfigRepository:
    - list_models(session) -> list[ModelConfig]
    - get_model(session, model_id: UUID) -> ModelConfig | None
    - create_model(session, data: CreateModelRequest) -> ModelConfig — encrypt api_key with Fernet (key from env var ENCRYPTION_KEY, fallback to app secret for MVP)
    - update_model(session, model_id: UUID, data: UpdateModelRequest) -> ModelConfig — if api_key field is empty/None, keep existing encrypted key per D-ADM11
    - delete_model(session, model_id: UUID) -> None
    - API key masking helper: mask_key(encrypted_key) -> "••••{last4}" per D-ADM11

    policy_repo.py — PolicyConfigRepository:
    - list_policies(session) -> list[PolicyConfig]
    - update_policy(session, scanner_name: str, enabled: bool, sensitivity: str) -> PolicyConfig
    - Seed default policies for known scanners on first access if table empty

    Expand backend/app/api/admin.py:
    - GET /api/admin/models — returns list of models with masked api_key
    - POST /api/admin/models — create model config
    - PUT /api/admin/models/{id} — update model config (api_key optional per D-ADM11)
    - DELETE /api/admin/models/{id} — delete model config
    - GET /api/admin/policy — returns list of scanner configs
    - PUT /api/admin/policy/{scanner_name} — update scanner enabled/sensitivity
    - All endpoints: Depends(get_admin_user), Pydantic request/response schemas
    - Response schema for models: NEVER include api_key_encrypted. Only include masked display "••••{last4}"
  </action>
  <verify>
    <automated>cd backend && python -c "from app.admin.models_repo import ModelConfigRepository; from app.admin.policy_repo import PolicyConfigRepository; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ModelConfig and PolicyConfig tables defined in schema.py
    - Alembic migration file exists for the new tables
    - GET /api/admin/models returns models with api_key masked as "••••{last4}"
    - POST/PUT /api/admin/models never returns full api_key in response
    - PUT with empty api_key field preserves existing encrypted key
    - GET /api/admin/policy returns scanner configs with enabled + sensitivity
    - All admin endpoints require admin role
  </acceptance_criteria>
  <done>Model and policy config CRUD API works with encrypted key storage and masked display</done>
</task>

<task type="auto">
  <name>Task 2: Frontend model config + policy config pages</name>
  <files>frontend/src/features/admin/ModelsPage.tsx, frontend/src/features/admin/PolicyPage.tsx, frontend/src/features/admin/components/ModelCard.tsx, frontend/src/features/admin/components/ModelEditModal.tsx, frontend/src/features/admin/components/ScannerRow.tsx, frontend/src/features/admin/components/Toast.tsx, frontend/src/features/admin/components/ConfirmDialog.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/features/admin/AdminLayout.tsx, frontend/src/features/admin/AuditPage.tsx, frontend/src/App.tsx, .planning/phases/04-streaming-admin-dashboard/04-UI-SPEC.md</read_first>
  <action>
    ModelsPage.tsx — per D-ADM09 and UI-SPEC:
    - Heading "模型供应商配置" (display typography 20px/600)
    - "添加模型" button (blue-600) top-right
    - Grid: 2 columns desktop, 1 mobile, gap 16px
    - Each model renders as ModelCard
    - useQuery for GET /api/admin/models
    - useMutation for POST/PUT/DELETE with query invalidation
    - On save success: Toast "配置已保存"

    ModelCard.tsx — per UI-SPEC:
    - bg-white border border-gray-200 rounded-lg p-16px
    - Shows: model name, provider_type, endpoint_url (truncated), "••••{last4}" for key, enabled/disabled badge
    - Disabled state: opacity-50
    - "编辑模型" button opens ModelEditModal

    ModelEditModal.tsx — per D-ADM07/D-ADM09:
    - Centered modal, max-w-480px, backdrop bg-black/50
    - Fields: name (text), provider_type (select: qwen/openai_compatible), endpoint_url (text), model_id (text), api_key (password field, label "API Key（留空则不修改）"), enabled (toggle)
    - "保存配置" button (blue-600), "放弃修改" cancel button
    - For edit mode: populate existing values, api_key field empty (placeholder shows masked value)
    - Changes not applied until Save per D-ADM07

    PolicyPage.tsx — per D-ADM10:
    - Heading "安全扫描器配置" (display typography)
    - List of ScannerRow components
    - useQuery for GET /api/admin/policy
    - "保存配置" button at bottom — collects all changes, sends PUT for each modified scanner
    - On save: Toast "配置已保存"

    ScannerRow.tsx — per D-ADM10 and UI-SPEC interaction states:
    - Scanner name (text), "启用" toggle (bg-green-600 when on, bg-gray-300 when off), sensitivity slider with 3 positions (低/中/高)
    - Slider visual: green for low, amber for medium, red for high per UI-SPEC

    Toast.tsx:
    - Fixed bottom-right, auto-dismiss after 3s, bg-green-600 text-white rounded-lg px-16px py-8px for success

    ConfirmDialog.tsx:
    - For destructive actions (disable/delete model): "确认禁用此模型？禁用后用户将无法选择该模型。"
    - Modal with confirm (red-600) and cancel buttons

    Update App.tsx:
    - Replace /admin/models and /admin/policy placeholder routes with ModelsPage and PolicyPage
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --strict 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ModelsPage renders 2-column grid of ModelCard components
    - ModelEditModal has password field labeled "API Key（留空则不修改）"
    - PolicyPage renders ScannerRow for each scanner with toggle and sensitivity slider
    - Toast shows "配置已保存" on successful save
    - ConfirmDialog appears for destructive model actions
    - /admin/models and /admin/policy routes render correct pages
    - TypeScript compiles without errors
    - All Chinese copywriting matches UI-SPEC exactly
  </acceptance_criteria>
  <done>Admin can configure model providers and scanner policies through dashboard UI with save confirmation and masked API keys</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> admin config endpoints | Admin CRUD operations modify system behavior |
| Stored API keys | Encrypted at rest, never returned in full |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | API key in responses | mitigate | Response schema masks key as "••••{last4}"; api_key_encrypted field excluded from serialization |
| T-04-07 | Elevation of Privilege | Config modification | mitigate | All config endpoints behind get_admin_user dependency |
| T-04-08 | Tampering | Policy config | mitigate | Pydantic validation: sensitivity must be "low"|"medium"|"high", enabled must be bool |
| T-04-SC | Tampering | npm/pip installs | mitigate | No new packages needed |
</threat_model>

<verification>
- GET /api/admin/models returns models with masked keys
- POST /api/admin/models creates new model config
- PUT with empty api_key preserves existing encrypted key
- GET /api/admin/policy returns scanner configs
- PUT /api/admin/policy/{name} updates enabled/sensitivity
- Frontend model cards display correctly with edit modal
- Frontend policy page shows toggles and sliders
- Save triggers Toast notification
</verification>

<success_criteria>
- Admin can add/edit/disable model providers with API keys securely stored
- Admin can enable/disable scanners and adjust sensitivity
- Configuration persists in database
- API keys never exposed in full in any response or UI
</success_criteria>

<output>
Create `.planning/phases/04-streaming-admin-dashboard/04-03-SUMMARY.md` when done
</output>
