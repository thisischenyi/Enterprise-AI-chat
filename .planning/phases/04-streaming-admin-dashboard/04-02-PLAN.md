---
phase: 04-streaming-admin-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/admin/__init__.py
  - backend/app/admin/audit_queries.py
  - backend/app/api/admin.py
  - frontend/src/features/admin/AdminLayout.tsx
  - frontend/src/features/admin/AdminSidebar.tsx
  - frontend/src/features/admin/AuditPage.tsx
  - frontend/src/features/admin/components/AuditStatCards.tsx
  - frontend/src/features/admin/components/AuditFilters.tsx
  - frontend/src/features/admin/components/AuditTable.tsx
  - frontend/src/features/admin/components/Pagination.tsx
  - frontend/src/features/admin/components/SparklineChart.tsx
  - frontend/src/features/admin/components/ActionBadge.tsx
autonomous: true
requirements: [ADMN-01]

must_haves:
  truths:
    - "Admin can view audit event metadata with risk categories, actions, timestamps, user and model info"
    - "Admin can filter audit events by time range, user, model, action, and risk category"
    - "Admin can paginate through audit events"
    - "Statistics cards show today's totals, block rate, and 7-day trend"
  artifacts:
    - path: "backend/app/admin/audit_queries.py"
      provides: "Paginated audit event queries with filters"
      exports: ["list_audit_events", "get_audit_stats"]
    - path: "backend/app/api/admin.py"
      provides: "GET /api/admin/audit and GET /api/admin/audit/stats endpoints"
    - path: "frontend/src/features/admin/AuditPage.tsx"
      provides: "Audit dashboard with stat cards, filters, and paginated table"
    - path: "frontend/src/features/admin/AdminLayout.tsx"
      provides: "Admin layout with sidebar navigation"
  key_links:
    - from: "frontend/src/features/admin/AuditPage.tsx"
      to: "/api/admin/audit"
      via: "TanStack useQuery"
      pattern: "useQuery.*admin/audit"
    - from: "backend/app/api/admin.py"
      to: "backend/app/admin/audit_queries.py"
      via: "function import"
      pattern: "list_audit_events"
---

<objective>
Deliver the admin audit viewer — statistics cards, multi-dimension filters, and paginated data table showing audit event metadata.

Purpose: Admins can monitor safety pipeline activity, see block rates, and investigate specific events (per D-ADM01 through D-ADM05, requirement ADMN-01).
Output: Working audit viewer with backend API and frontend dashboard UI.
</objective>

## Phase Goal

**As a** admin, **I want to** view audit event metadata through a dashboard with statistics and filters, **so that** I can monitor safety pipeline effectiveness and investigate incidents.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-streaming-admin-dashboard/04-RESEARCH.md
@.planning/phases/04-streaming-admin-dashboard/04-UI-SPEC.md
@backend/app/audit/repository.py
@backend/app/audit/events.py
@backend/app/api/admin.py
@backend/app/auth/current_user.py
@frontend/src/features/admin/AdminStubPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend audit query API with pagination and filters</name>
  <files>backend/app/admin/__init__.py, backend/app/admin/audit_queries.py, backend/app/api/admin.py</files>
  <read_first>backend/app/audit/repository.py, backend/app/audit/events.py, backend/app/api/admin.py, backend/app/db/schema.py, backend/app/auth/current_user.py</read_first>
  <action>
    Create backend/app/admin/ package (if not exists).

    audit_queries.py — query functions per D-ADM01 through D-ADM05:
    - list_audit_events(session, time_range="today", user_id=None, model_id=None, action=None, risk_category=None, page=1, page_size=20) -> tuple[list[AuditEvent], int]
      - time_range: "today" | "7d" | "30d" — filter created_at accordingly
      - Returns (events, total_count) for pagination
      - Order by created_at DESC
    - get_audit_stats(session, time_range="today") -> AuditStats
      - Returns: total_events, total_blocks, block_rate_percent, most_active_user_id, daily_counts_7d (list of {date, count} for sparkline)

    Expand backend/app/api/admin.py:
    - GET /api/admin/audit — query params: time_range, user_id, model_id, action, risk_category, page, page_size. Returns {events: [...], total: int, page: int, page_size: int}
    - GET /api/admin/audit/stats — query param: time_range. Returns stats object.
    - Both endpoints use Depends(get_admin_user) for auth.
    - Response schemas: AuditEventResponse (id, timestamp, user_id, model_id, action, risk_categories, scanner_results metadata), AuditStatsResponse.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.admin.audit_queries import list_audit_events, get_audit_stats; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/admin/audit_queries.py exports list_audit_events and get_audit_stats
    - GET /api/admin/audit returns paginated audit events with total count
    - GET /api/admin/audit/stats returns total_events, total_blocks, block_rate_percent, most_active_user_id, daily_counts_7d
    - Both endpoints require admin role (get_admin_user dependency)
    - time_range filter supports "today", "7d", "30d"
  </acceptance_criteria>
  <done>Admin audit API endpoints serve paginated, filtered audit data with statistics</done>
</task>

<task type="auto">
  <name>Task 2: Frontend admin layout + audit viewer page</name>
  <files>frontend/src/features/admin/AdminLayout.tsx, frontend/src/features/admin/AdminSidebar.tsx, frontend/src/features/admin/AuditPage.tsx, frontend/src/features/admin/components/AuditStatCards.tsx, frontend/src/features/admin/components/AuditFilters.tsx, frontend/src/features/admin/components/AuditTable.tsx, frontend/src/features/admin/components/Pagination.tsx, frontend/src/features/admin/components/SparklineChart.tsx, frontend/src/features/admin/components/ActionBadge.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/features/admin/AdminStubPage.tsx, frontend/src/features/chat/ConversationSidebar.tsx, frontend/src/App.tsx, frontend/src/lib/api.ts, .planning/phases/04-streaming-admin-dashboard/04-UI-SPEC.md</read_first>
  <action>
    AdminLayout.tsx — per D-ADM08/D-ADM12 and UI-SPEC layout contract:
    - Flex row: AdminSidebar (220px fixed, bg-gray-50 border-r) + content area (flex-1, p-24px, overflow-y-auto)
    - Renders React Router Outlet for nested routes

    AdminSidebar.tsx — per UI-SPEC interaction states:
    - 3 nav items: "审计日志" (/admin/audit), "模型配置" (/admin/models), "安全策略" (/admin/policy)
    - Active state: bg-white, left border 3px blue-600, font-weight 600
    - Hover: bg-gray-100
    - Use NavLink from react-router for active detection
    - Lucide icons: Shield for audit, Server for models, Settings for policy

    AuditPage.tsx — per D-ADM01 through D-ADM05:
    - Page heading "审计事件" (20px/600 per UI-SPEC display typography)
    - AuditStatCards at top
    - AuditFilters below
    - AuditTable below filters
    - State: time_range, filters object, page number
    - useQuery for /api/admin/audit (with filter params) and /api/admin/audit/stats (with time_range)
    - Time range toggle updates both cards and table per D-ADM03

    AuditStatCards.tsx — per D-ADM02 and UI-SPEC:
    - 5 cards in flex row, gap 16px, each flex-1 min-w-180px
    - Cards: "今日事件" (total), "今日拦截" (blocks, red-600 if >0), "拦截率" (percent, amber-500 if >20%), "最活跃用户" (user_id), "7日趋势" (SparklineChart)
    - Card style: bg-gray-50 border border-gray-200 rounded-lg p-24px
    - Stat value: 28px/600 per UI-SPEC

    AuditFilters.tsx — per D-ADM04:
    - Flex row, gap 16px, items-center, mb-16px
    - Time range toggle buttons: "今天" / "近7天" / "近30天"
    - Dropdowns: "用户", "模型", "动作" (允许/拦截/兜底拦截), "风险类别"
    - Filter style: border-gray-300 rounded h-36px text-14px

    AuditTable.tsx — per D-ADM05 and UI-SPEC:
    - Columns: timestamp, user, model, action (ActionBadge), risk categories, details
    - Full width, border border-gray-200 rounded-lg
    - Row hover: bg-gray-50, row height min 44px
    - Table header: bg-gray-50
    - Empty state: "暂无审计事件" / "当用户发送消息后，审计记录将显示在此处。"
    - Pagination component below table

    Pagination.tsx:
    - Page numbers + prev/next per D-ADM05
    - Active page: bg-blue-600 text-white rounded

    SparklineChart.tsx:
    - Simple SVG sparkline (no charting library) showing 7 data points
    - Polyline with stroke blue-600, viewBox sized to fit card

    ActionBadge.tsx:
    - "允许" green (bg-green-50 text-green-700), "拦截" red (bg-red-50 text-red-700), "兜底拦截" amber (bg-amber-50 text-amber-700)

    Update App.tsx routing:
    - Add /admin route with AdminLayout element
    - Nested routes: /admin/audit (AuditPage), /admin/models (placeholder), /admin/policy (placeholder)
    - Default redirect /admin -> /admin/audit
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --strict 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AdminLayout renders sidebar (220px) + content outlet
    - AdminSidebar has 3 nav items with Chinese labels matching UI-SPEC copywriting
    - AuditPage renders stat cards, filter bar, and data table
    - Stat cards show 28px font-weight-600 values per UI-SPEC
    - Filter bar has time range toggle + 4 dropdown filters
    - Table shows audit events with ActionBadge coloring
    - Pagination renders page numbers with blue-600 active state
    - SparklineChart renders SVG polyline
    - /admin/audit route is accessible and renders AuditPage
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Admin audit viewer is fully functional with stats, filters, paginated table, and proper Chinese copywriting</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> GET /api/admin/* | Admin-only endpoints; non-admin must get 403 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | Admin audit endpoints | mitigate | get_admin_user dependency on all admin routes returns 403 for non-admin |
| T-04-05 | Information Disclosure | Audit event details | mitigate | Audit events contain metadata only (no raw prompts/outputs per SAFE-06) |
| T-04-SC | Tampering | npm/pip installs | mitigate | No new packages; existing deps only |
</threat_model>

<verification>
- GET /api/admin/audit returns paginated events (admin auth required)
- GET /api/admin/audit/stats returns statistics
- Non-admin gets 403 on admin endpoints
- Frontend renders stat cards, filters, table with correct Chinese labels
- Pagination navigates between pages
- Time range toggle updates both stats and table
</verification>

<success_criteria>
- Admin can view audit events with timestamps, users, models, actions, risk categories
- Admin can filter by time range, user, model, action, risk category
- Statistics cards show today's counts and 7-day trend sparkline
- Pagination works for browsing events
</success_criteria>

<output>
Create `.planning/phases/04-streaming-admin-dashboard/04-02-SUMMARY.md` when done
</output>
