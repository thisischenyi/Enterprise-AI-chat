---
phase: 03-conversation-history-chat
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/features/chat/ConversationSidebar.tsx
  - frontend/src/features/chat/ChatPage.tsx
  - frontend/src/stores/chatStore.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04]
must_haves:
  truths:
    - "User sees sidebar with list of past conversations sorted by most recent"
    - "User can click a conversation to load its messages and continue chatting"
    - "User can start a new conversation via button that clears chat state"
    - "Sending a message in new conversation shows it in sidebar after response"
    - "Blocked messages show block notification but do not appear in conversation history on reload"
  artifacts:
    - path: "frontend/src/features/chat/ConversationSidebar.tsx"
      provides: "Conversation list sidebar with date groups"
      min_lines: 40
    - path: "frontend/src/features/chat/ChatPage.tsx"
      provides: "Updated layout with sidebar + chat area"
    - path: "frontend/src/stores/chatStore.ts"
      provides: "Extended store with activeConversationId"
    - path: "frontend/src/lib/api.ts"
      provides: "fetchConversations and fetchConversationMessages API functions"
  key_links:
    - from: "frontend/src/features/chat/ConversationSidebar.tsx"
      to: "/api/conversations"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*conversations"
    - from: "frontend/src/features/chat/ChatPage.tsx"
      to: "/api/conversations/:id/messages"
      via: "TanStack Query for message loading on conversation select"
      pattern: "fetchConversationMessages"
---

<objective>
Frontend conversation sidebar and chat integration: browse past conversations, resume them, and see new conversations appear after sending messages.

Purpose: Deliver the user-facing conversation history experience per the UI design contract.
Output: Working sidebar with conversation list, resume capability, and new conversation flow integrated with the backend from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conversation-history-chat/03-RESEARCH.md
@.planning/phases/03-conversation-history-chat/03-PATTERNS.md
@.planning/phases/03-conversation-history-chat/03-UI-SPEC.md
@.planning/phases/03-conversation-history-chat/03-01-SUMMARY.md

<interfaces>
From frontend/src/lib/api.ts:
- apiClient<T>(path, options) helper for authenticated requests
- Existing sendMessage(model_id, message) pattern

From frontend/src/stores/chatStore.ts:
- Zustand create<ChatState> pattern
- Existing: messages, isLoading, addMessage, setLoading

From frontend/src/features/chat/ChatPage.tsx:
- Existing flex column layout with ModelSelector, ChatMessages, ChatInput
- useMutation for sendMessage, useQuery for models

Expected from Plan 01 backend:
- GET /api/conversations -> ConversationSummary[]
- GET /api/conversations/:id/messages -> MessageResponse[]
- POST /api/chat/send now returns conversation_id, message_id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API functions + chatStore extension</name>
  <files>frontend/src/lib/api.ts, frontend/src/stores/chatStore.ts</files>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/stores/chatStore.ts
  </read_first>
  <action>
Add to api.ts:
- Type ConversationSummary: { id: string; title: string; model_id: string; updated_at: string }
- Type MessageResponse: { id: string; role: string; content: string; created_at: string }
- Update ChatResponse type to include conversation_id: string | null and message_id: string | null
- fetchConversations(): Promise of ConversationSummary[] — GET /api/conversations
- fetchConversationMessages(id: string): Promise of MessageResponse[] — GET /api/conversations/{id}/messages
- Update sendMessage to accept optional conversation_id parameter, include in request body

Extend chatStore.ts:
- Add activeConversationId: string | null (default null)
- Add setActiveConversation(id: string | null): sets activeConversationId and clears messages array
- Add startNewConversation(): sets activeConversationId to null and clears messages
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - api.ts exports fetchConversations and fetchConversationMessages functions
    - api.ts ConversationSummary type has id, title, model_id, updated_at fields
    - chatStore exports useChatStore with activeConversationId state
    - setActiveConversation clears messages array when switching conversations
    - sendMessage accepts optional conversation_id parameter
  </acceptance_criteria>
  <done>API layer and state management ready for conversation sidebar integration</done>
</task>

<task type="auto">
  <name>Task 2: ConversationSidebar component</name>
  <files>frontend/src/features/chat/ConversationSidebar.tsx</files>
  <read_first>
    - frontend/src/features/chat/ChatPage.tsx
    - .planning/phases/03-conversation-history-chat/03-UI-SPEC.md
  </read_first>
  <action>
Create ConversationSidebar.tsx per UI design contract:

Layout: w-[280px] fixed, bg-gray-50, border-r border-gray-200, flex flex-col, h-screen.

Structure:
- Top: "New conversation" button (bg-blue-600 text-white, full width minus padding, hover bg-blue-700). Calls startNewConversation from chatStore.
- Below: scrollable conversation list via useQuery({ queryKey: ["conversations"], queryFn: fetchConversations })
- Group conversations by date: "Today", "Yesterday", "Previous 7 days", then month names. Use simple date comparison logic.
- Each ConversationItem: padding 8px vertical / 16px horizontal, title truncated single line (text-ellipsis overflow-hidden whitespace-nowrap), relative date as label text (12px, gray-500).
- Active state: bg-gray-100 + left border 3px blue-600. Compare item.id with activeConversationId from chatStore.
- Hover state: bg-gray-100.
- Empty state: "No conversations yet" heading + "Start a new conversation to begin chatting with AI." body text.
- Loading state: skeleton placeholder lines.
- Responsive: hidden below 768px (md:flex), hamburger toggle button visible on mobile.

On conversation click: call setActiveConversation(id).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ConversationSidebar.tsx exists with useQuery for ["conversations"]
    - "New conversation" button calls startNewConversation
    - Conversations grouped by date with labels "Today", "Yesterday", "Previous 7 days", month names
    - Active conversation has bg-gray-100 and left border blue-600
    - Sidebar width is 280px, hidden below md breakpoint
    - Empty state shows "No conversations yet" text
  </acceptance_criteria>
  <done>Conversation sidebar displays user's conversations with proper styling and interaction states</done>
</task>

<task type="auto">
  <name>Task 3: ChatPage layout integration + conversation resume + send wiring</name>
  <files>frontend/src/features/chat/ChatPage.tsx</files>
  <read_first>
    - frontend/src/features/chat/ChatPage.tsx
    - frontend/src/features/chat/ConversationSidebar.tsx
    - frontend/src/stores/chatStore.ts
  </read_first>
  <action>
Restructure ChatPage layout to flex row (per D-UI01):
- Outer div: flex h-screen
- Left: ConversationSidebar component
- Right: flex-1 flex flex-col (existing chat content)

Add conversation resume behavior:
- useEffect watching activeConversationId: when it changes to a non-null value, fetch messages via fetchConversationMessages(id) and populate chatStore messages array. Use useQuery with enabled: !!activeConversationId for the messages fetch.
- When activeConversationId is null, show empty chat (new conversation mode).

Update send mutation:
- Include activeConversationId in sendMessage call as conversation_id parameter.
- On successful send response: if response has conversation_id and activeConversationId was null, call setActiveConversation(response.conversation_id) to track the newly created conversation.
- Invalidate ["conversations"] query on successful send (updates sidebar list and order).

Model selector: when resuming conversation, auto-select the conversation's model_id (from ConversationSummary). When starting new, user picks model freely.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ChatPage renders ConversationSidebar + chat area in flex row layout
    - Clicking a conversation in sidebar loads its messages into the chat display
    - Sending a message passes conversation_id to backend
    - New conversation: first send creates conversation, updates activeConversationId from response
    - Sidebar refreshes (query invalidation) after successful send
    - Conversation's model_id used when resuming
  </acceptance_criteria>
  <done>Full conversation history flow works: browse sidebar, resume past conversations, start new ones, messages persist across page refreshes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> UI | Conversation data displayed directly — no user-controlled HTML injection risk (React escapes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Sidebar | accept | Backend already enforces user ownership — frontend only sees own data |
| T-03-05 | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- Chat page shows sidebar on left with conversation list
- Clicking "New conversation" clears chat and allows fresh message
- Sending a message creates a conversation visible in sidebar
- Clicking an existing conversation loads its message history
- Continuing a conversation appends messages correctly
- Blocked messages do not appear in conversation on page reload
- Sidebar hidden on mobile, visible on desktop
</verification>

<success_criteria>
Complete conversation history UI: employees can browse past conversations in sidebar, resume any conversation with full history loaded, start new conversations, and see the sidebar update in real time as they chat. Storage boundary visually confirmed — blocked content never appears in loaded history.
</success_criteria>

<output>
Create `.planning/phases/03-conversation-history-chat/03-02-SUMMARY.md` when done
</output>
