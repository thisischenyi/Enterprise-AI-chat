---
phase: 03-conversation-history-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/schema.py
  - backend/app/conversations/repository.py
  - backend/app/api/conversations.py
  - backend/app/api/chat.py
  - backend/app/main.py
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04]
must_haves:
  truths:
    - "GET /api/conversations returns user's conversations sorted by updated_at desc"
    - "GET /api/conversations/:id/messages returns all messages for a conversation"
    - "POST /api/chat/send with allowed content creates Conversation (lazy) and Message rows"
    - "POST /api/chat/send with blocked input creates no Message or Conversation rows"
    - "POST /api/chat/send returns conversation_id and message_id in response"
    - "Model receives last 20 messages as context for multi-turn conversations"
  artifacts:
    - path: "backend/app/db/schema.py"
      provides: "Conversation and Message SQLAlchemy models"
      contains: "class Conversation"
    - path: "backend/app/conversations/repository.py"
      provides: "Conversation CRUD operations"
      exports: ["ConversationRepository"]
    - path: "backend/app/api/conversations.py"
      provides: "Conversation list and message fetch endpoints"
      exports: ["router"]
    - path: "backend/app/api/chat.py"
      provides: "Extended chat send with conversation persistence"
  key_links:
    - from: "backend/app/api/chat.py"
      to: "backend/app/conversations/repository.py"
      via: "ConversationRepository for lazy create + message persist"
      pattern: "ConversationRepository"
    - from: "backend/app/api/conversations.py"
      to: "backend/app/conversations/repository.py"
      via: "list and fetch queries"
      pattern: "ConversationRepository"
---

<objective>
Backend conversation persistence: DB models, repository, API endpoints, and chat send extension.

Purpose: Enable conversation history storage and retrieval so the frontend can display past conversations and resume them.
Output: Working API endpoints for conversation list, message fetch, and persistent chat with lazy conversation creation and storage boundary enforcement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conversation-history-chat/03-RESEARCH.md
@.planning/phases/03-conversation-history-chat/03-PATTERNS.md
@.planning/phases/02-safety-pipeline-chat/02-03-SUMMARY.md

<interfaces>
From backend/app/db/schema.py (existing models pattern):
- Base class: DeclarativeBase
- Uses Mapped[uuid.UUID], mapped_column(Uuid, primary_key=True, default=uuid.uuid4)
- ForeignKey("users.id") pattern for user references

From backend/app/api/chat.py (existing send endpoint):
- ChatRequest: model_id: str, message: str
- ChatResponse: status: str, message: str | None, block_message: str | None, risk_categories: list[str]
- Uses get_current_user, get_db_session dependencies
- SafetyPipeline integration: input_scan -> model call -> output_scan -> audit

From backend/app/audit/repository.py (repository pattern):
- class AuditRepository(__init__(session: AsyncSession))
- Uses self._session.add() + await self._session.flush()
- SQLAlchemy 2 select() style queries

From backend/app/models/providers.py:
- ModelProvider.chat_completion(messages: list[dict[str, str]]) -> str
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conversation and Message DB models + Alembic migration</name>
  <files>backend/app/db/schema.py, backend/alembic/versions/</files>
  <read_first>
    - backend/app/db/schema.py
    - backend/alembic.ini
    - backend/alembic/env.py
  </read_first>
  <action>
Add Conversation and Message models to schema.py following existing Base/Mapped pattern (per D-DM01):
- Conversation: id (UUID PK), user_id (FK users.id, indexed), title (String 100), model_id (String 100), created_at (datetime), updated_at (datetime with onupdate)
- Message: id (UUID PK), conversation_id (FK conversations.id), role (String 20 — "user"|"assistant"), content (Text), created_at (datetime)
- Message composite index: ix_messages_conversation_created on (conversation_id, created_at)

Then run alembic revision --autogenerate -m "add_conversations_and_messages" to generate migration. Verify migration file looks correct.
  </action>
  <verify>
    <automated>cd backend && alembic upgrade head && python -c "from app.db.schema import Conversation, Message; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/db/schema.py contains class Conversation(Base) with __tablename__ = "conversations"
    - backend/app/db/schema.py contains class Message(Base) with __tablename__ = "messages"
    - Message has Index("ix_messages_conversation_created", "conversation_id", "created_at")
    - Alembic migration file exists in backend/alembic/versions/
    - alembic upgrade head succeeds without error
  </acceptance_criteria>
  <done>Conversation and Message tables exist in DB with correct schema and indexes</done>
</task>

<task type="auto">
  <name>Task 2: ConversationRepository + API endpoints</name>
  <files>backend/app/conversations/__init__.py, backend/app/conversations/repository.py, backend/app/api/conversations.py, backend/app/main.py</files>
  <read_first>
    - backend/app/audit/repository.py
    - backend/app/api/chat.py
    - backend/app/main.py
  </read_first>
  <action>
Create backend/app/conversations/__init__.py (empty).

Create backend/app/conversations/repository.py with ConversationRepository class (session: AsyncSession):
- create_conversation(user_id, title, model_id) -> Conversation
- list_for_user(user_id) -> list[Conversation] ordered by updated_at desc
- get_conversation(conversation_id, user_id) -> Conversation | None (filter by user_id for ownership)
- add_message(conversation_id, role, content) -> Message
- get_messages(conversation_id) -> list[Message] ordered by created_at asc
- update_timestamp(conversation_id) -> None (set updated_at to now)

Create backend/app/api/conversations.py with APIRouter prefix="/api/conversations":
- GET "/" — list_conversations: returns list of ConversationSummary(id, title, model_id, updated_at) for current user
- GET "/{conversation_id}/messages" — get_messages: returns list of MessageResponse(id, role, content, created_at), validates user owns conversation (403 if not)

Pydantic response models: ConversationSummary, MessageResponse. Serialize UUID as str, datetime as ISO string.

Register router in backend/app/main.py alongside existing routers.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.api.conversations import router; print(router.routes)"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/conversations/repository.py contains class ConversationRepository with create_conversation, list_for_user, get_conversation, add_message, get_messages methods
    - backend/app/api/conversations.py contains GET "/" and GET "/{conversation_id}/messages" endpoints
    - Both endpoints use Depends(get_current_user) for auth
    - GET "/{conversation_id}/messages" returns 403 if conversation.user_id != current_user.id
    - Router registered in main.py with prefix="/api/conversations"
  </acceptance_criteria>
  <done>Conversation list and message fetch endpoints respond correctly with auth enforcement</done>
</task>

<task type="auto">
  <name>Task 3: Extend POST /chat/send with conversation persistence and storage boundary</name>
  <files>backend/app/api/chat.py</files>
  <read_first>
    - backend/app/api/chat.py
    - backend/app/conversations/repository.py
  </read_first>
  <action>
Modify ChatRequest to add conversation_id: str | None = None.
Modify ChatResponse to add conversation_id: str | None = None and message_id: str | None = None.

Extend the send endpoint logic (per D-SB01, D-SB02, D-RC02):

1. If conversation_id provided: load conversation via repository, verify user ownership (403 if not). Load last 20 messages to use as model context (per D-RC02).
2. Build messages list for model call: loaded history + new user message, in [{role, content}] format (per D-RC03).
3. Input scan — if blocked: audit event only, return block response with no conversation_id/message_id. NO Message row created (per D-SB01, HIST-04).
4. If input allowed and no conversation_id: lazy-create Conversation with title = first 50 chars of message (per D-DM02, D-SB02).
5. Create Message(role="user") row.
6. Call model with full messages context.
7. Output scan — if blocked: audit event, return block response. User message already stored (acceptable per research pitfall 1). Include conversation_id in response.
8. Create Message(role="assistant") row.
9. Update conversation.updated_at.
10. Return response with conversation_id and message_id.

Use ConversationRepository instantiated with the db session from Depends.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.api.chat import ChatRequest, ChatResponse; assert hasattr(ChatRequest.model_fields, 'conversation_id'); assert hasattr(ChatResponse.model_fields, 'conversation_id'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ChatRequest has conversation_id: str | None = None field
    - ChatResponse has conversation_id: str | None = None and message_id: str | None = None fields
    - Blocked input path: no Message row created, no Conversation row created, audit event recorded
    - Allowed input with no conversation_id: Conversation created with title = message[:50]
    - Model called with last 20 messages from conversation history + new message
    - Allowed output: both user and assistant Message rows created
    - conversation.updated_at updated after new messages
  </acceptance_criteria>
  <done>Chat send persists conversations with lazy creation, sends history as model context, and enforces storage boundary for blocked content</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->API | conversation_id from client could reference another user's conversation |
| API->DB | Must enforce user ownership on all conversation access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | GET /conversations/:id/messages | mitigate | Verify conversation.user_id == current_user.id before returning messages |
| T-03-02 | Tampering | POST /chat/send conversation_id | mitigate | Verify ownership before allowing message append to conversation |
| T-03-03 | Tampering | npm/pip installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- GET /api/conversations returns 200 with empty list for new user
- POST /api/chat/send with allowed message returns conversation_id in response
- GET /api/conversations then returns the created conversation
- GET /api/conversations/:id/messages returns the messages in order
- POST /api/chat/send with blocked input returns no conversation_id, no Message rows exist
- GET /api/conversations/:id/messages with wrong user returns 403
</verification>

<success_criteria>
All conversation CRUD endpoints work with auth enforcement. Storage boundary proven: blocked content never creates Message rows. Multi-turn context sent to model provider.
</success_criteria>

<output>
Create `.planning/phases/03-conversation-history-chat/03-01-SUMMARY.md` when done
</output>
